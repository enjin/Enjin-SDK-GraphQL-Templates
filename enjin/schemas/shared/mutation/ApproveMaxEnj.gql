query {
    result: ApproveMaxEnj(
        wallet: $ethAddress,
        test: $test,
        send: $send
    ) {
        ...Transaction
    }
}

$ethAddress: EthAddress
$test: Boolean = true
$send: Boolean = true
