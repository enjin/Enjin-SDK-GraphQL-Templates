#namespace enjin.sdk.project.BridgeClaimAsset
#import enjin.sdk.shared.Transaction

#arg address EthAddress!
#arg assetId String

mutation {
    result: BridgeClaimAsset(
        address: $address
        assetId: $assetId
    ) {
        ...Transaction
    }
}