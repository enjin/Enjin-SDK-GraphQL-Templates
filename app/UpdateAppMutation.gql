mutation {
    result: UpdateEnjinApp(
        id: $id,
        name: $name,
        description: $description,
        image: $image,
        options: $options
    ) {
        ...AppFragment
    }
}

$id: Int
$name: String
$description: String
$image: String
$options: AppOptionsInput